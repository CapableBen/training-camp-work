<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <properties>

    </properties>

    <settings>
        <setting name="logImpl" value="LOG4J"/>
    </settings>

    <typeAliases>

    </typeAliases>

    <plugins>
        <plugin interceptor="com.geekbang.springmybatis.work.interceptor.MyInterceptor">
            <property name="name" value="value"/>
            <!--<my:extension></my:extension>-->
        </plugin>
    </plugins>

    <!--<environments default="development">
        <environment id="development">
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${driver}"/>
                <property name="url" value="${url}"/>
                <property name="username" value="${username}"/>
                <property name="password" value="${password}"/>
            </dataSource>
        </environment>
    </environments>-->

    <mappers>
        <!--        <mapper resource="classpath*:mybatis/*.xml"/>-->
        <!--        <mapper class="com.geekbang.springmybatis.work.mappers.UserMapper"/>-->
        <!--        <package name="com.geekbang.springmybatis.work.mappers"/>-->
    </mappers>

</configuration>